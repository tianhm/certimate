此目录下存放对依赖包的裁剪，以使构建的二进制轻量化。

目录结构与原依赖包的模块结构保持一致。

---

This directory stores partial copies of third-party dependencies, to lightweight the binaries.

Maintains a 1:1 mapping to the original dependency's module tree.
